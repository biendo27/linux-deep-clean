# Go build and test artifacts only. Keep plans, provenance, fixtures, and
# generated evidence visible to Git so they cannot be hidden accidentally.
/bin/
/coverage/
/coverage-integration/
/coverage.out
*.coverprofile
*.prof
*.test
